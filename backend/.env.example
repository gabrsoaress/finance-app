# Nunca coloque esta chave em appsettings.json nem em controlo de versões.
ConnectionStrings__Supabase=Host=aws-0-REGION.pooler.supabase.com;Port=5432;Database=postgres;Username=postgres.PROJECT_REF;Password=SUBSTITUIR;SSL Mode=Require;Trust Server Certificate=true

# Quando existir um parceiro AISP/Open Banking:
OpenBanking__Provider=SUBSTITUIR
OpenBanking__ClientId=SUBSTITUIR
OpenBanking__ClientSecret=SUBSTITUIR

# Quando existir uma app Meta WhatsApp Cloud API:
WhatsApp__VerifyToken=SUBSTITUIR
WhatsApp__AppSecret=SUBSTITUIR
